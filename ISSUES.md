# Issues

Audit trail of bugs. Each entry: date, area, description, root cause (**code bug** vs **test bug**), status (Open / Fixed) + commit on resolution.

| Date | Area | Description | Root cause | Status | Resolved by |
| --- | --- | --- | --- | --- | --- |
| 2026-05-17 | Frontend (transactions) | Slot ID `-1` renders literally as `S-1`. ESPN uses `-1` for "no slot" (the player wasn't in a lineup before/after). | Code bug — `app.js:txItemLine` concatenates `"S" + slot` without filtering sentinel values. | Fixed | (this commit) |
| 2026-05-17 | Frontend (transactions) | Team ID `0` renders as `T0`. ESPN uses `0` for "no team" (free-agent pool / not a team-change). | Code bug — same function. | Fixed | (this commit) |
| 2026-05-17 | Frontend (transactions) | Slot IDs rendered as raw `S1` / `S6` instead of position labels (G / UTIL / BE). | Code bug — no slot label map in JS. | Fixed | (this commit) |
| 2026-06-01 | Pipeline (news) | Non-WNBA articles appear in "Recent WNBA News" tab. E.g. "Odell Beckham Jr. is the latest star to return to old stomping grounds" at position #1. ESPN's news endpoint returns general multi-sport articles alongside WNBA content. | Code bug — `pipeline/news.py` fetches a feed that isn't WNBA-scoped; no sport filter applied before storing `news_recent`. Fix: filter by known WNBA `pro_team_ids` before writing to state. | Fixed | `pipeline/news.py` WNBA_TEAM_IDS filter (commit 913cc76) |
| 2026-06-01 | Pipeline (news) | False player tag: DeWanna Bonner (player_id=869) tagged on the OBJ article. ESPN's OBJ story includes `athlete_ids=[869, ...]` — ID 869 is an NFL/NBA athlete in that context but collides with Bonner's WNBA player_id. The `match_to_players` lookup is correct by design but the cross-sport article that contains the ID should never reach the feed. Root cause is the same as the non-WNBA article bug above. | Code bug — upstream: ESPN athlete IDs are not sport-scoped; the fix is rejecting non-WNBA articles before matching. | Fixed | Same commit as UAT-004 |
| 2026-06-01 | Frontend (mobile nav) | "Transactions" tab label truncates to "Transact" at 375px viewport. Tab buttons have `flex-shrink: 1`, so they compress when the total label width (Waivers + Team Needs + News + Transactions = 372px) exceeds the nav container (343px at 375px). The nav has `overflow-x: auto` so it could scroll, but the buttons shrink first. | Code bug — CSS on `.tab` missing `flex-shrink: 0`; padding also too wide for 375px. | Fixed | `flex-shrink: 0` added; `@media (max-width:420px) { .tab { padding-inline:8px } }` added (commit 913cc76 + this session) |
| 2026-06-01 | Frontend (player modal) | Team-tagged news articles show in "Recent News" without any label. Articles about the player's team (game previews/recaps) are indistinguishable from articles directly mentioning the player — misleading. E.g. Teja Oblak's modal showed "Expansion Fire beat the Fever 100-84, limiting Caitlin Clark to 6 points" as top news. | Code bug — `populatePlayerModal` rendered all `entry.news` the same; no differentiation between `athlete_ids` direct match vs team-tag. | Fixed | "TEAM NEWS" badge added when `p.player_id` not in `n.athlete_ids` (`app.js`) |
| 2026-06-01 | Frontend (player modal) | 5-stat free-agent modal has a 3+2 orphan row where Season and Ownership boxes stretch to ~50% width each (vs ~33% for the top row of 3 stats). | Code bug — `repeat(auto-fit, minmax(72px, 1fr))` collapses the empty 3rd track in row 2, causing the 2 orphan items to fill 2/3 of the row. | Fixed | `statsWrap.style.gridTemplateColumns = "repeat(3, 1fr)"` in `populatePlayerModal` forces a fixed 3-column grid |
| 2026-06-01 | Frontend (waivers, mobile) | Waiver-card player names clipped to "Jade M" / "Elizabe" at 375px. The `28px 1fr auto` grid let the two-block schedule eat the width, squeezing the name column to ~100px. | Code bug — `style.css .waiver-card` had no responsive reflow. | Fixed | (this commit) — grid-areas drop the schedule to its own row below 480px |
| 2026-06-01 | Frontend (team needs, mobile) | Team-detail "Top picks" rows clipped names AND the bucket pill (G/F/C) overlapped the team abbreviation at 375px. Same squeeze as the waiver card. | Code bug — `.team-target-row` `20px 1fr auto auto` grid with no mobile reflow. | Fixed | (this commit) — grid-areas drop schedule to its own row below 480px |
| 2026-06-01 | Frontend (formatting) | Ownership change rendered as `-0.0%` for values that round to zero (e.g. `-0.004`). | Code bug — `app.js:fmtPctChange` applied a `-` sign before rounding. | Fixed | (this commit) — collapse `|v| < 0.05` to `0` before formatting |
| 2026-06-01 | Data integrity | Committed `docs/data/state.json` (captured 21:07Z) was newer than the committed raw snapshot `data/raw/2026-06-01/league.json` (17:45Z) — the final refresh rebuilt state but never re-committed the raw league/free-agent JSON, so state was derived from data absent from the audit trail. The missing 21:07Z raw pull was later found uncommitted in the main worktree. | Process bug — the refresh skill's final commit updated `state.json` without the matching raw files. | Fixed | (this commit) — committed the recovered 21:07Z raw snapshot (league/free_agents/news/_meta + the 2 appended `FUTURE_ROSTER` txns) and rebuilt `state.json` from it via `scripts/rebuild_state.py`, so raw↔state are now consistent at the fresher snapshot with no data lost |
| 2026-06-01 | Environment | `pip` env missing `eval_type_backport`; pydantic v2 on Python 3.9 then can't evaluate `str \| None` annotations → import crash, all tests error at collection. | Setup bug — dep pinned in `requirements.txt` but not installed in the active interpreter. | Fixed | (this commit) — `pip install eval_type_backport==0.2.0` |
| 2026-06-05 | Skill (Twitter/X scrape) | `javascript_tool` does not support bare top-level `await` — syntax error at runtime even though docs suggest it works. Need `(async()=>{...})()` wrapper. | Process bug — undocumented platform constraint; previous skill assumed top-level await. | Fixed | SKILL.md step 4c updated with async IIFE pattern |
| 2026-06-05 | Skill (Twitter/X scrape) | `window._tweets` accumulator cleared on every page navigation — cannot persist tweet data across player searches using window globals. | Process bug — window context is reset on navigate; localStorage persists across same-domain navigations. | Fixed | SKILL.md step 4c updated to use `localStorage.setItem('ta', '[]')` accumulation pattern |
| 2026-06-05 | Skill (Twitter/X scrape) | `javascript_tool` output filter blocks full tweet JSON arrays containing `url` fields with Twitter status links ("cookie/query string data" false positive). Base64-encoded blobs also blocked. Fetch to localhost also blocked by x.com CSP `connect-src` policy. | Process bug — multiple exfiltration channels blocked; need two-phase metadata+title extraction then Python reconstruction. | Fixed | SKILL.md step 4d updated with metadata-first extraction + Python reconstruction pattern |
| 2026-06-05 | Skill (refresh working dir) | SKILL.md hardcoded `/Users/pranava/Documents/Projects/FantasyGM` — wrong path, project lives at `/Users/pranava/Projects/FantasyGM`. | Process bug — stale path copied from initial setup, never corrected. | Fixed | SKILL.md line 22 and step 1 bash command corrected |
| 2026-07-16 | Pipeline (lineups) | `check_lineup` reported `status: "set"` while simultaneously reporting 95.7 points of headroom. A roster shorter than the nine active slots leaves a slot *empty*, so the fix is "start her" with nobody benched — but ins were paired to outs with `zip()`, which silently drops a move that has no counterpart. Hit team Nut (9 players, 8 starting) on the real 2026-07-06 snapshot. | Code bug — `zip()` truncates to the shorter list; the dropped move is the most valuable kind, since an empty slot scores zero. | Fixed | `lineups.py` emits `action: "start"` for empty slots; `tests/test_lineups.py::TestEmptySlot` asserts status never contradicts headroom (commit 271e171) |
| 2026-07-16 | Pipeline (lineups) | Week-horizon lineup check judged "no game" from a single scoring period, which would label a 3-games-this-week player gameless because she was idle on the window's first day. | Code bug — the plays-predicate wasn't horizon-aware. | Fixed | `has_games()` uses `games_this_week` over a week horizon; regression test in `TestTonightHorizon` (commit 271e171) |
| 2026-07-16 | Frontend (Today) | Win-probability estimate rendered "52% rough odds" beside a live "up 50.0" lead — a number visibly contradicting the one above it. The normal approximation over `matchup_history` variance only models which team is better *on the season*; it cannot see the in-progress score. | Code bug (design) — the model answers a different question than the one the UI implies. An honest matchup win-probability needs remaining-schedule modelling that `state.json` doesn't carry. | Fixed | Estimate removed; Today shows both sides' season averages instead. Proper win odds backlogged (commit ce0ac9f) |
| 2026-07-16 | Frontend (nav) | Bottom tab bar and team chip were inert until a ~2.8 MB `state.json` fetch completed — `main()` awaits the fetch before `initNav` bound their listeners, so early taps silently did nothing on a slow connection. The top tabs were unaffected because they already used a parse-time delegated listener. | Code bug — per-element listeners bound after an `await`. | Fixed | All chrome moved to the same parse-time delegation (commit ce0ac9f) |
| 2026-07-16 | Frontend (a11y) | Team-picker tiles and bottom-nav buttons exposed **no accessible name** — each is an `aria-hidden` icon span plus a label span, and name-from-contents didn't resolve. Screen-reader users would hear a row of unlabeled buttons. Caught by reading the accessibility tree, not the screenshot. | Code bug — missing explicit labels on icon+label buttons. | Fixed | Explicit `aria-label` on every picker tile and nav button (commit ce0ac9f) |
| 2026-07-16 | Pipeline (trades) | 12 of 85 players in generated trade packages are OUT and priced at their full healthy per-game rate, so an unavailable player is offered at full value with no hint she's hurt. The lineup checker refuses to *start* an OUT player while the trade generator recommends *acquiring* one — the two disagree. Surfaced when Today promoted the trade opener to the default tab (it offered Olivia Miles, OUT). | Code bug — `trades.py` filters neither availability nor discounts the projection. | **Open** — display mitigated (status now shown next to the price on both surfaces, commit ce0ac9f); repricing belongs to Trades 2.0 (P4). See BACKLOG. |
