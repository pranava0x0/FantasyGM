# Issues

Audit trail of bugs. Each entry: date, area, description, root cause (**code bug** vs **test bug**), status (Open / Fixed) + commit on resolution.

| Date | Area | Description | Root cause | Status | Resolved by |
| --- | --- | --- | --- | --- | --- |
| 2026-05-17 | Frontend (transactions) | Slot ID `-1` renders literally as `S-1`. ESPN uses `-1` for "no slot" (the player wasn't in a lineup before/after). | Code bug — `app.js:txItemLine` concatenates `"S" + slot` without filtering sentinel values. | Fixed | (this commit) |
| 2026-05-17 | Frontend (transactions) | Team ID `0` renders as `T0`. ESPN uses `0` for "no team" (free-agent pool / not a team-change). | Code bug — same function. | Fixed | (this commit) |
| 2026-05-17 | Frontend (transactions) | Slot IDs rendered as raw `S1` / `S6` instead of position labels (G / UTIL / BE). | Code bug — no slot label map in JS. | Fixed | (this commit) |
| 2026-06-01 | Frontend (waivers, mobile) | Waiver-card player names clipped to "Jade M" / "Elizabe" at 375px. The `28px 1fr auto` grid let the two-block schedule eat the width, squeezing the name column to ~100px. | Code bug — `style.css .waiver-card` had no responsive reflow. | Fixed | (this commit) — grid-areas drop the schedule to its own row below 480px |
| 2026-06-01 | Frontend (team needs, mobile) | Team-detail "Top picks" rows clipped names AND the bucket pill (G/F/C) overlapped the team abbreviation at 375px. Same squeeze as the waiver card. | Code bug — `.team-target-row` `20px 1fr auto auto` grid with no mobile reflow. | Fixed | (this commit) — grid-areas drop schedule to its own row below 480px |
| 2026-06-01 | Frontend (formatting) | Ownership change rendered as `-0.0%` for values that round to zero (e.g. `-0.004`). | Code bug — `app.js:fmtPctChange` applied a `-` sign before rounding. | Fixed | (this commit) — collapse `|v| < 0.05` to `0` before formatting |
| 2026-06-01 | Data integrity | Committed `docs/data/state.json` (captured 21:07Z) was newer than the committed raw snapshot `data/raw/2026-06-01/league.json` (17:45Z) — the final refresh rebuilt state but never re-committed the raw league/free-agent JSON, so state was derived from data absent from the audit trail. | Process bug — the refresh skill's final commit updated `state.json` without the matching raw files. | Fixed | (this commit) — `scripts/rebuild_state.py` regenerates state deterministically from the on-disk raw snapshot, restoring raw↔state consistency (cost: 2 `FUTURE_ROSTER` lineup-shuffle txns dropped from the recent feed, still present in `transactions.jsonl`) |
| 2026-06-01 | Environment | `pip` env missing `eval_type_backport`; pydantic v2 on Python 3.9 then can't evaluate `str \| None` annotations → import crash, all tests error at collection. | Setup bug — dep pinned in `requirements.txt` but not installed in the active interpreter. | Fixed | (this commit) — `pip install eval_type_backport==0.2.0` |
